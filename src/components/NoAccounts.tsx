import React from "react";
import './NoAccounts.scss';

export default function NoAccounts() {
    // TODO: ADD ACCOUNT MENU MAYBE HIDE PLUS IN HEADER
    return <div id="no_accounts">
        <span>
            You don't have any accounts yet. Click on the plus sign in the top left corner to create an account.
        </span>
    </div>
}